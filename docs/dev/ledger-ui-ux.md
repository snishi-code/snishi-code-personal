# simple-ledger — UI / UX

medical 側のコードは参照せず、snishi-code 系の抽象 UI 規約に従う。実装は
`simple-ledger-src/src/ui/`、文言は `src/i18n/ja.ts`。

## 全体方針

- スプレッドシート風 UI にしない。操作入口を少なくする。
- 入力の主導線は **ホームの 収入 / 支出 / 振替 の 3 ボタンのみ**（ヘッダーに `+` は置かない）。
  補助操作はメニュー / 設定に集約。
- 白背景・控えめな線・teal を主色。角丸は控えめ（`--radius: 12px`）。
- タップ領域は **44px 以上**（`--tap: 44px`、`.icon-btn` / `.btn` / `.list__item` など）。
- **色だけに依存しない**: 状態はアイコン + 文言を併用（エラー=⚠+文、成功=✓+文、増減=記号 +/−）。
- 成功は toast。危険操作は明示確認。保存 / import / 削除は fail-closed。

## 画面構成（`src/ui/screens/`）

- **Dashboard**（初期表示・期間の財務ダッシュボード）: H1 は視覚的に期間ラベル（`periodLabel`。
  アクセシブル名は「ホーム」）。上部に **収入 / 支出 / 振替** の 3 入力ボタン（唯一の入力起点）。
  その下に **期間切替**（`PeriodSwitcher`: 月別 / 年別 / 全体。月は `input[type=month]`、年は select。
  独自カレンダーは作らない）。期間の収支（収入/支出/収支）と財政状態（資産/負債/純資産）は
  **項目ごとにタップ可能**で PL/BS の該当セクションへスクロール遷移。**フロー（PL）は期間合計、
  BS は期間末の基準日（`periodAsOf`: 月→月末 / 年→年末 / 全体→最終データ日 or 今日）**で出す
  （フローとストックを混ぜない）。年別 / 全体では **推移ブロック**（各月の収支バー + 各月末の純資産バー。
  チャートライブラリは使わず CSS バー。色だけに依存せず数値も併記）を出す。
  **生活コスト**（通常支出 / 月額化コスト / 合計）の領域全体は **資金計画・負債への導線**。
  下部に **期間内の仕訳**（最大 5 件・日付降順、生成仕訳も表示）と「すべて見る」（期間フィルタ付きで
  仕訳画面へ）。期間は App の共有 state（`ReportPeriod`）で、ホーム / 財務諸表 / 仕訳に伝播する。
- **Journal**: 仕訳一覧。検索（摘要・メモ）・期間・**勘定科目絞り込み**（PL/BS からの遷移）。
  行タップで編集、各行に **取消/返金**（逆仕訳）と削除。
- **Entry**（シート）: 入力モード（収入/支出/振替/詳細）。日常入力は**お金の流れ `源泉 → 行き先`**で
  見せ、**借方/貸方や資産/負債/収益/費用の分類見出しは出さない**。並びは人間の入力順
  （**日付 → 項目 → 金額 → お金の流れ（A → B）→ 詳細**）。内部対応は常に source=貸方(credit) /
  destination=借方(debit): 収入 `収入元 → 入る場所`、支出 `支払い方法 → 使い道`（現金/預金/カードを
  並列に flat 表示）、振替 `移動元 → 移動先`（項目は任意・未入力なら「移動元 → 移動先」を自動付与）。
  候補は科目の **`role`** で絞る。`FlowPicker`（`AccountPicker` の flat モード + 矢印 `→`）を使う。
  メモ・タグは「詳細」折りたたみに収める。
  科目は**チップピッカー**（`AccountPicker`、単一選択 radio）。「詳細入力」で借方/貸方直接指定
  （manual）へ切替可。取消/返金は逆仕訳をプリセット。日付の既定は今日（必須）。支出では
  「**月額化する**」トグル + 何か月分・継続/買い替えで月額化コストを登録（種類は入力から推定。
  月額化 ON ではタグ欄を隠す）。支払い元が負債のときだけ、支払い元の近くに「分割/後日引落を
  資金繰りに入れる」を出し、引落口座・返済回数・初回引落日（購入日と別）を入力できる。
- **月額化コスト**（`Allocations.tsx`、画面名は月額化コスト）: サブスク・年払い・耐久財・定期
  イベントを「月あたりコスト」で一覧。既定は active のみ、停止/終了はトグル表示。種類・総額・
  月額・何か月分・更新周期・今月の計上額・費用カテゴリ・支払い元・状態を表示。一時停止/再開・
  削除ができる。登録時に**実際の支払い仕訳**を作り、生活コストは formula で月割り表示する
  （二重計上はしない）。
- **Statements**: PL / BS をセグメント切替。個人家計向けに用語は **収入 / 支出 / 収支**
  （内部型名は `revenue`/`expense`/`netIncome` のまま）。**PL は 収入・支出 を独立表示し、
  差引収支（= 収入合計 − 支出合計）は別枠サマリーとして出す**（収支を収入/支出カテゴリに混ぜない。
  `収支` 行はドリルダウンしない）。BS は左右対照（左 資産 / 右 負債＋純資産で左右合計一致）。区分色は
  CSS variables（`--fs-asset` 等、既存トークン参照）。モバイルは縦積み。**期間切替（`PeriodSwitcher`）は
  ホームと共有**（`ReportPeriod`）。PL は期間合計、BS は期間末の基準日（`periodAsOf`）で導出し、基準日は
  文言で表示する（手入力の基準日ピッカーは廃止）。**科目行をタップすると Journal へドリルダウン**。
  ホームからは該当セクション
  （`fs-revenue`/`fs-expense`/`fs-net`/`fs-assets`/`fs-liabilities`/`fs-equity`）へスクロール。
- **Accounts**: 勘定科目を区分ごとに残高つきで管理（役割 `role` も表示）。追加 / 編集 /
  アーカイブ / 削除（参照中は不可）。シートで `type` と `role` を選ぶ（`type` 変更時は `role` を
  既定へリセット、`type` と矛盾する `role` は保存不可）。**使用中の科目は区分(type)を変更不可**
  （UI で無効化、`upsertAccount` でも fail-closed）。**`role` は使用中でも変更可**（会計残高は
  変わらず入力候補が変わるだけ）。「使用中」判定は仕訳/予定CF/目的別資金/按分の参照を含む。
- **Cashflow（資金計画・負債）**: 「今後の支払い予定」（自由資金推移）、**負債・分割払い**
  （支払用負債ごとに次回支払日・残額・件数を集約）、**目的別資金**、**資金目標**（長期の積立計画。
  目標額・期限・現在額・**必要な毎月の積立額**＝期待年利を仮定して導出）を扱う。長期ローン専用
  スキーマは作らず、既存 `CashflowSchedule` と負債科目から見える範囲に限定する。
  **予定入力は日常入力と同じ `日付 / 項目 / 金額 / お金の流れ(A → B)`**。入金/出金/移動はロールから推定する
  （`inferScheduleFlow`: 収入カテゴリ→日常資産=inflow、日常資産→費用カテゴリ/支払用負債=outflow、
  **日常資産→日常資産=transfer（将来の口座間移動。自由資金の総額は変えない）**、それ以外（負債→費用など
  現金移動が一意でない組み合わせ）は推定不能として保存拒否）。`accountId`=現金が動く資産
  （transfer では移動元）、もう一方を `counterAccountId`（transfer では移動先）に保存する。実績化は
  inflow=借方 account/貸方 counter、outflow/transfer=借方 counter/貸方 account（transfer は 借方 移動先 /
  貸方 移動元）。`projectCashflow` は transfer では総資金・自由資金を増減させない（一覧では中立表示「→」）。
- **Settings**: 先頭に **「管理」セクション**（勘定科目 / タグ / 残高補正 への遷移。
  財務諸表はホームの PL/BS サマリーから辿るため含めない）。
  続いて JSON export / import、スナップショット、台帳設定（**期待年利%** を含む）、アプリ情報、全データ削除。
  期待年利は資金目標の必要積立額の参考計算にのみ使う（投資助言ではない・既定 0）。
- **Help**: 使い方の説明（モーダル）。

## ヘッダー（`src/ui/Header.tsx`）

- 左: ホームアイコン（→ Dashboard）
- 中央: `YYYY年MM月`（主表示）/ 台帳名（副題・小さく）。**月切り替えは未実装のためボタン風にしない**。
- 右: `≡`（メニュー）

入力は **ホームの 3 ボタンに集約**し、ヘッダーに `+` は置かない（入力の種類シートも廃止）。

## メニュー（`src/ui/Menu.tsx`）

右からのドロワー（**管理・補助機能に絞る**）: **月額化コスト / 資金計画・負債 / 設定 / ヘルプ**。
ホーム=ヘッダーのホームアイコン、仕訳=ホーム下部「当月の仕訳」やドリルダウン、財務諸表=ホームの
PL/BS から辿る（メニューには置かない）。勘定科目 / タグ / 残高補正は**設定の「管理」セクション**。

## モーダル / シート規約（`src/ui/Modal.tsx`）

- 閉じ方は **`dismissMode`** で統一する（旧 `dismissable` は後方互換: `false`=`never` 相当）:
  - **`always`**: メニュー / ヘルプ / 軽い選択。背景タップ・`Escape` で閉じる（選んだら閉じる）。
  - **`if-clean`**: 入力フォーム。未編集なら閉じ、**編集済み（dirty）は破棄確認**を出す
    （`useDirtyGuard`。「入力を破棄しますか？」→ 破棄する / 編集を続ける。確認自体は `never`）。
    dirty 判定は各シートで入力フィールドのスナップショットを初期値と比較する。`×` / 背景タップ /
    `Escape` / フッターのキャンセルはすべて `requestClose` を通す。
  - **`never`**: 削除 / 全削除 / import / 復元など破壊的操作。背景タップ・`Escape` で**閉じない**。
- 背景タップ判定は **`onPointerDown` かつ `e.target === e.currentTarget`**（内部要素のタップでは閉じない）。
- 保存 / キャンセルがあるフォームは下部に明確なボタン。フォーカストラップ + 復帰、`role="dialog"` /
  `aria-modal`。

## toast（`src/ui/toast.tsx`）

- 成功 = success（teal）、失敗 = error（赤）。アイコン + 文言。`role="status"` / `aria-live="polite"`。
- **保存失敗時に成功 toast を出さない**（store の各アクションは失敗時 error toast + 例外送出）。

## 危険操作（`src/ui/ConfirmDialog.tsx`）

- 削除 / import 上書き / 復元は確認ダイアログ。確定ボタンは danger 色 + ⚠。
- 全データ削除は **キーワード入力一致**（「削除」と入力）まで確定を無効化。

## アクセシビリティ

- 主要操作は `getByRole` / `getByLabelText` で辿れるよう、ラベルと control を id で結ぶ。
- フォーム入力は 16px 以上（iOS のズーム回避）。
- コントラストは WCAG AA を満たす（teal-700 を白文字ボタン背景に使用）。
- スキップリンク、`:focus-visible` のフォーカスリング。
- E2E で axe-core の critical/serious 違反ゼロを検証（`e2e/a11y.spec.ts`）。

## 関連

- 安定セレクタ: [ui-contract.md](ui-contract.md)
- トークン: [design-system.md](design-system.md)
